#DataSource config
spring.datasource.url=${DATASOURCE_TEST_URL}
spring.datasource.username=${DATASOURCE_TEST_USER}
spring.datasource.password=${DATASOURCE_TEST_PASSWORD}

spring.jpa.hibernate.ddl-auto=update
